{% extends 'base.html.twig' %}

{% block title %}TestEval index{% endblock %}

{% block body %}
    <h1>TestEval index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for test_eval in test_evals %}
            <tr>
                <td>{{ test_eval.id }}</td>
                <td>{{ test_eval.nom }}</td>
                <td>{{ test_eval.prenom }}</td>
                <td>{{ test_eval.description }}</td>
                <td>
                    <a href="{{ path('test_eval_show', {'id': test_eval.id}) }}">show</a>
                    <a href="{{ path('test_eval_edit', {'id': test_eval.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('test_eval_new') }}">Create new</a>
{% endblock %}
