{% extends 'base.html.twig' %}

{% block title %}TestEval{% endblock %}

{% block body %}
    <h1>TestEval</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_eval.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ test_eval.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ test_eval.prenom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ test_eval.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_eval_index') }}">back to list</a>

    <a href="{{ path('test_eval_edit', {'id': test_eval.id}) }}">edit</a>

    {{ include('test_eval/_delete_form.html.twig') }}
{% endblock %}
