

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta property="og:url"           content="https://www.sf2.m3104.petit.com/video" />
    <meta property="og:type"          content="Website" />
    <meta property="og:title"         content="Hello TPHomeController!" />
    <meta property="og:description"   content="Description" />
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://bootswatch.com/5/darkly/bootstrap.min.css">

    <link rel="stylesheet" type="text/css" href="{{asset('css/style.css')}}">



    <title>{% block title %}{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}

    {% endblock %}

    {% block javascripts %}
        {#{{ encore_entry_script_tags('app') }}#}
    {% endblock %}
</head>
<body>


<nav>
    <ul id="navbar">
        <li class="nav"><a href="{{ path("pt_home") }}">Accueil</a></li>
        <li class="nav"><a href="{{ path("record") }}">Records</a></li>
        <li class="nav"><a href="{{ path("news") }}">Actualités</a></li>

        <li class="nav"><a href="{{ path("action") }}">Action de la semaine</a></li>

        <li class="nav"><a href="{{ path("video") }}">Vidéos</a></li>

        <li class="nav"><a href="{{ path("wiki") }}">Wiki</a></li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav"><a href="{{ path("admin") }}">Pannel Admin</a></li>
        {% endif %}
        {% if is_granted('ROLE_SUPER_ADMIN') %}
            <li class="nav"><a href="{{ path("superadmin") }}">Pannel Admin</a></li>
        {% endif %}
        {% if app.user %}
            <div class="lastNav">
        <li class="nav"><a href="{{ path("logout") }}">{{ app.user.username }} </a></li>
            </div>
        {% endif %}
    </ul>

</nav>

{% block header %}


{% endblock %}
{% block body %}


{% endblock %}

</body>
</html>


