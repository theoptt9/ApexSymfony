{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>

            <tr>
                <th>Titre</th>
                <td>{{ video.titre }}</td>
            </tr>
            <tr>
                <th>LienVide</th>
                <td>{{ video.lienVide }}</td>
            </tr>
            <tr>
                <th>Desciption</th>
                <td>{{ video.desciption }}</td>
            </tr>
            <tr>
                <th>IdTag</th>
                <td>{{ video.idTag }}</td>
            </tr>
            <tr>
                <th>DateVide</th>
                <td>{{ video.dateVide ? video.dateVide|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('video') }}">back to list</a>

    <a href="{{ path('video_edit', {'id': video.id}) }}">edit</a>

    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
